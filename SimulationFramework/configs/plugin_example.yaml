simulation: example_sim

params:
  world_size: 25
  num_agents: 50

evolution:
  population_size: 200
  mutation_rate: 0.02
  crossover_rate: 0.7
  elite_fraction: 0.05
  random_seed: 1234

logging:
  log_interval: 10
  checkpoint_interval: 100
  experiment_name: "baseline_test"
